    <div class="text-center">
      <h1>おすすめの撮影スポットサイト</h1>
      <p>写真好きの写真好きによる</br>
      写真好きの為のサイトです📸
      </p>
    </div>
    <div class="row">
      <% @posts.each do |post| %>
      <div class="col-3">
        <%= link_to post_path(post) do %>
        <div>
          <p>
            <% if post.image.attached? %>
            <%= image_tag post.image.variant(resize: "250x250").processed, class: "img-fluid" %>
          <% else %>
          <%= image_tag "no_image", size: "80x80" %>
        <% end %>
          </p>
        </div>
      <% end %>
      </div>
    <% end %>
    </div>
    <p class="text-right">
      <%= link_to "全ての投稿を見る>", posts_path %>
    </p>